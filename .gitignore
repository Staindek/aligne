# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDEs
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo

# Node (defensive — sub-projects have their own ignores)
node_modules/

# Env files (defensive — never commit secrets at root)
.env
.env.*
!.env.example

# Logs
*.log
logs/

# Terraform (Phase 3)
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# AWS / cloud credentials (defensive — these belong in ~/.aws, never in repo)
.aws/
credentials
*.pem
*.ppk

# Build / coverage at root (defensive)
dist/
build/
coverage/
